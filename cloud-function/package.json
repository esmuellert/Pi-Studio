{
  "name": "cloud-function",
  "version": "1.0.0",
  "description": "Cloud Function used by Pi Studio project. Currently, the Cloud Function used to add a record to Datastore when an image file has been successfully uploaded to Cloud Storage.",
  "main": "index.js",
  "scripts": {
    "start": "npx functions-framework --target=onImageUploaded --signature-type=event --port=8000"
  },
  "author": "Yanuo Ma",
  "license": "ISC",
  "devDependencies": {
    "@google-cloud/functions-framework": "^1.9.0",
    "prettier": "^2.4.1"
  }
}
